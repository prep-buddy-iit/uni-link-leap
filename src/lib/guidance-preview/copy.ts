/**
 * Pre-trial CTA copy.
 *
 * Deliberately names the price. This screen sits in front of a paid trial, so
 * the word "free" must never appear on the button - see the guard in
 * `guidance-preview.test.tsx`.
 */
export const TRIAL_CTA_COPY = "Get your full breakdown - start your ₹99, 3-day trial";
